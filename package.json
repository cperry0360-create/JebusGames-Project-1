{
  "name": "courjahan-defense",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Courjahan Defense — a roguelite tower defense game. JebusGames.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
